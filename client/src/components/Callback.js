import React , { PureComponent } from 'react';

class Callback extends PureComponent {

    render () {
        return(
            <div>
                Loading...
            </div>
        )
    }
}

export default (Callback);